# Example channel profile — real ones are named <channel_id>.conf and gitignored (they encode
# your viewing history). The script auto-creates a stub on first encounter with a new channel.
#
# Declare the domain to pull in profiles/domains/<domain>.conf vocabulary:
#DOMAIN=tech
# Channel-specific terms (people, project names, recurring jargon of this channel):
CHANNEL_TERMS=""
