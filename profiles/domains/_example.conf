# Example domain profile — real ones are named <domain>.conf and gitignored (your domains
# reflect your interests; the engine ships neutral). A channel profile opts in via DOMAIN=<name>.
#
# BASE_PROMPT opens Whisper's initial_prompt; DOMAIN_TERMS are appended (~224-token cap total).
#BASE_PROMPT="Tech podcast."
#DOMAIN_TERMS="Kubernetes, PostgreSQL, GraphQL, Rust, WebAssembly, Nvidia, CUDA, PyTorch"
